<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.code.forge">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity$page1" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_sub"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Syllabus"
            android:label="@string/title_activity_syllabus"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TeachingPlan"
            android:label="@string/title_activity_teaching_plan"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Schedules"
            android:label="@string/title_activity_schedules"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".InstructionalMaterials"
            android:label="@string/title_activity_instructional_materials"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Courses"
            android:label="@string/title_activity_courses"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>